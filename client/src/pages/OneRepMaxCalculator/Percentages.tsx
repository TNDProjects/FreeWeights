import type { Max } from "../OneRepMaxCalculator/types/types.ts";
import useGetPercentages from "./utils/getWeightAtPercentage";

const Percentages = (oneRepMax: Max) => {
  const percentages = useGetPercentages(oneRepMax);




}
