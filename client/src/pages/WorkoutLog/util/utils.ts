import type { WorkoutLogRow } from "../types/types.ts";


//combine reps and sets into one field so we can use it inside the table.

interface repsAndSets {

}
const combineRepsAndSets = (sets: number, reps: number) => {


}
